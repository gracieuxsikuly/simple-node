language: node_js
node_js:
- 13

services:
- docker

# Prétester les installations
install:
- echo "nothing needs to be installed"

# Scripts à exécuter en guise de tests
before_script:
- echo "no tests"

script:
- docker --version # print the version for logging
- docker build -t simple-nodes .
- docker tag simple-nodes gracieuxsikuly/simple-node:latest

after_success:
- echo "HubDocker@Gracieux" | docker login -u "gracieuxsikuly" --password-stdin
- docker push gracieuxsikuly/simple-node